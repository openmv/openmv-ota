"""Chunked-stream helpers (stub).

aligned_chunks() and friends for the updater: yield write-aligned chunks from a
network/file stream so rom_ioctl WRITE calls land on flash write boundaries. TODO.
"""
