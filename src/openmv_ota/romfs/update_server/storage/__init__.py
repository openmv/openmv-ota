"""Object-storage adapters (stub).

A generic adapter interface over S3 / R2 / GCS / Azure Blob / local filesystem,
plus a small metadata DB (Postgres or SQLite) and the append-only transparency
log. TODO: define the adapter interface and a local-filesystem implementation.
"""
